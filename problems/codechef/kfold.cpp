#include <bits/stdc++.h>
#define N 1005
using namespace std;

int t, n, k;
char S[N];

int main(void) {
  scanf("%d", &t);
  while (t--) {
    scanf("%d %d", &n, &k);
    for (int i = 0; i < n; ++i) scanf("%d", &S[i]);
  }
  return 0;
}
